package org.sunrise.dangdang.test;import javax.mail.*;import javax.mail.internet.InternetAddress;import javax.mail.internet.MimeMessage;import java.io.*;import java.util.Date;import java.util.Properties;public class EmailTest {    public static void main(String[] args) throws MessagingException, IOException {        // 开启debug调试        Properties props = new Properties();        Session session = Session.getInstance(props);        Message message = new MimeMessage(session);        message.setFrom(new InternetAddress("986182669@qq.com","Admin","UTF-8"));        message.setRecipient(MimeMessage.RecipientType.TO,new InternetAddress("2726677650@qq.com","USER_CC","UTF-8"));        message.setSubject("测试");        message.setContent("Test邮件正文","text/html;charset=utf-8");        message.setSentDate(new Date());        message.saveChanges();        OutputStream out = new FileOutputStream("E:/Email/Mail.eml");        message.writeTo(out);        out.flush();        out.close();    }}